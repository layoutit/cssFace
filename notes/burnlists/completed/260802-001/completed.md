# Completed Work

- B1 | 2026-08-02T20:51:48-03:00 | Tracked all 326 FacesJS ids and 30 `FaceConfig` paths, froze the 35-id allowlist and 25/18 preset denominators, recorded both generated package layouts, and captured a real-pointer Chrome trace with 1,883 leaves and active sparse rotation lighting. Validated with `pnpm check:facesjs-catalog && pnpm baseline:facesjs -- --check`.
- B2 | 2026-08-02T21:01:04-03:00 | Added source-hash-bound component/catalog contracts, strict path/layer/dependency validation, browser-safe catalog and manifest loaders, a four-fixture bridge kept outside reusable components, and five focused tests. Validated with `pnpm build:prepare && pnpm test:facesjs` plus the catalog generator check.
- B3 | 2026-08-02T21:26:44-03:00 | Replaced preset-baked RGB lighting with neutral diffuse/specular texel atlases, assigned every leaf to one of eight root-controlled material roles, and added arbitrary validated color updates without topology, atlas, or network churn. Real-Chromium proof kept 1,883 leaf identities stable through recolor/orbit with zero canvases, zero added resources, `runtimeLightingMath: 0`, and active prepared lighting; four fixture captures measured 1.53-2.19 RGB MAE with p95 deltas of 10-13. Validated with `pnpm test:facesjs && pnpm check`.
- B4 | 2026-08-02T21:51:30-03:00 | Added strict full-`FaceConfig` validation, the 16-layer upstream transform descriptor, source bounds/formulas, signed eye/brow endpoint targets, piecewise nose sizing, corrected body sizing, runtime config application, and source-derived head-shave cells. Real-Chromium min/max/orbit proof retained all leaf identities with active prepared lighting, 48 shave leaves, zero canvases, and zero console errors. Validated with `pnpm test:facesjs && pnpm check`.
- B20 | 2026-08-02T22:01:50-03:00 | Replaced fatness-driven projected-feature x drift with fixed source anchors and prepared bilinear fatness interactions for eye, brow, and nose transforms. Real Chromium measured left/right eye-center drift below 0.0005px from fatness 0 to 1 while retaining 2,466 leaf identities, surface clearance, active lighting, zero canvases, and zero console errors. Validated with `pnpm test:facesjs && pnpm check`.
- B5 | 2026-08-02T22:30:04-03:00 | Prepared all 35 eyes, 30 eyebrows, 17 mouths, and 17 noses through one source-order compiler, including circle/ellipse SVG nodes, exact paints and strokes, paired/flip/angle/size/Pinocchio states, strict source/hash/package contracts, deterministic 2.2 MB output, and 25,797 conformed triangles with at least 0.72px clearance. Validated with `pnpm check:facesjs-catalog && pnpm test:facesjs && pnpm prepare:model -- --check`.
- B6 | 2026-08-02T22:43:26-03:00 | Prepared all 7 eye-line, 5 smile-line, 11 miscellaneous-line, and 83 facial-hair ids as source-hash-bound overlays, including four explicit empty variants, translucent/blended source paint, paired smile sizing, facial-hair fatness, clamped edge attachment, and non-overlapping display-layer lifts. The reusable catalog now contains 205 components and 85,764 triangles. Validated with `pnpm check:facesjs-catalog && pnpm test:facesjs && pnpm prepare:model -- --check`.
- B7 | 2026-08-02T22:49:11-03:00 | Prepared all seven glasses ids as source-shaped raised components, including the explicit empty variant, inherited group stroke semantics, translucent lens paint, primary/secondary frame roles, whole-feature fatness, and a layer-14 lift above every prepared facial feature. The reusable catalog now contains 212 components and 90,682 triangles. Validated with `pnpm check:facesjs-catalog && pnpm test:facesjs`.
- B8 | 2026-08-02T23:17:35-03:00 | Replaced modulo/RFL substitution with 21 distinct source-contour head shells and three paired source-contour ear shells. Each package is source-hash-bound, manifold, consistently wound, closed at source, fatness, and ear-size endpoints, preserves source skin/shave roles within 0.75px front-silhouette error, and identifies the adapter-authored depth and rear closure. The four compatibility fixtures now use a coarse source-contour bridge nested beneath hair while retaining the full 179,442-triangle reusable component set. Validated with `pnpm check:facesjs-catalog && pnpm test:facesjs && pnpm prepare:model -- --check`.
- B9 | 2026-08-02T23:31:04-03:00 | Prepared all five body ids as source-contour closed skin shells and all eighteen jersey ids as independently selectable closed raised regions. The 90 body/jersey compositions share one attachment contract and body-size state, retain source team paint and tank-top openings, keep at least 6.78px body clearance, omit only the unwanted silhouette stroke, expose no package dependencies, and contain no boundary, non-manifold, or duplicate shell cells. The reusable catalog now contains 259 components and 235,062 triangles. Validated with `pnpm check:facesjs-catalog && pnpm test:facesjs && pnpm prepare:model -- --check`.
- B10 | 2026-08-02T23:35:41-03:00 | Classified all 51 hair and 8 hair-background ids exactly once into 2 empty/bald, 10 cap, 11 fade, 14 raised-mass, 7 rear/long, and 15 background-coupled outcomes. The tracked strategy catalog binds every row to its source hash, measurable bounds/gradient evidence, one of six named geometry contracts, explicit front/rear/weld requirements, symmetric background counterparts, and the exact upstream four-hat substitute-or-hide rule. Validated with `pnpm check:facesjs-catalog && pnpm check:facesjs-hair`.
- B11 | 2026-08-03T00:04:03-03:00 | Prepared 36 reusable hair components covering all 10 cap, 11 fade, 14 raised-mass, and the intentional bald id. Each package retains exact source paint and gradient stops on the front, flip/fatness endpoints, upstream hat substitution metadata, a source-intersecting attachment across all 21 heads, and a closed non-degenerate rear half-shell instead of the rejected diamond extrusion. The inspected 18-view contact sheet at `.local/facesjs-hair/b11-contact-sheet.png` covers short, fade, and afro strategies across representative head/fatness variants. The catalog now contains 295 components and 310,699 triangles. Validated with `pnpm check:facesjs-hair && pnpm test:facesjs && pnpm prepare:model -- --check`.
- B12 | 2026-08-03T00:33:56-03:00 | Prepared the remaining 15 front-hair and eight hair-background ids, completing all 51 `hair` and eight `hairBg` packages. The eight coupled front styles resolve only their exact upstream background dependency, seven rear-long styles keep separate front-paint/rear-shell groups, background layers remain at layer 0 behind body/head, and intersecting shoulders retain at least 5.11px clearance. The asymmetric full shell replaced the rejected center-plane profile seam, and the inspected 18-view sheet at `.local/facesjs-hair/b12-contact-sheet.png` covers four coupled and two standalone long strategies across front/side/rear views. The catalog contains 318 reusable components and no head/body/hair combinations. Validated with `pnpm check:facesjs-hair && pnpm test:facesjs && pnpm prepare:model -- --check`.
- B13 | 2026-08-03T00:56:27-03:00 | Prepared all eight accessory ids as one explicit empty outcome, one raised projected eye-black component, two closed swept headband shells, and four closed volumetric hat shells. Every non-empty accessory mounts on layer 15 with at least 2.64px front clearance; each hat embeds the exact 51-style upstream substitute-short, substitute-short-fade, or hide partition without combination packages. The inspected 18-view sheet at `.local/facesjs-accessories/b13-contact-sheet.png` verifies front/side/rear volume and suppresses projected paint on rear views. The catalog contains 326 reusable components and 512,409 prepared triangles. Validated with `pnpm check:facesjs-catalog && pnpm test:facesjs && pnpm prepare:model -- --check`.
- B19 | 2026-08-03T03:19:26-03:00 | Replaced the four full-face compatibility packages with one tiny `facesjs-component-face` base plus 326 source-hash-bound, independently fetchable prepared components. Classic, Afro, Bald, and Short2 now compose through the same lazy graph; changing Classic to Afro fetches only the selected dependency delta; the legacy model rows/directories and `public/faces` are absent; and no head-by-hair or body-by-jersey combinations exist. Offline preparation emits 594,841 prepared triangles, while runtime composition retains selected resources, model identity, material roles, morphs, and prepared rotation lighting. Real Chromium equivalence passed at IoU 0.953, 0.927, 0.958, and 0.953 respectively. Validated with `pnpm build:prepare && pnpm prepare:model -- --check && pnpm test:facesjs && pnpm baseline:facesjs -- --component-equivalence`.
- B14 | 2026-08-03T04:19:01-03:00 | Added one strict resolver for fixtures and arbitrary complete `FaceConfig` objects, typed unsupported/dependency errors, exact FacesJS hat/hair selection semantics, canonical JSON/share serialization, and the documented `cssface-uniform-catalog-v1` generator. All 326 catalog components now include independently loadable prepared assets; a 10,000-seed sweep is deterministic and resolvable, while a 121-seed directed set reaches every id. Oversized lighting sheets use deterministic 8,192px-bounded pages and zero-area derived runtime cells are dropped without changing source geometry. Real Chromium proved seed 0 loaded exactly 16 selected components, seed 1234 fetched only its 14-component cache delta, and a 1,326-character share URL reloaded the exact config with zero errors. Validated with `pnpm test:facesjs && pnpm check && pnpm build` (56 tests) plus `pnpm prepare:model -- --check`.
- B15 | 2026-08-03T04:42:09-03:00 | Added a deterministic 157-case source-semantic corpus that reaches every one of the 326 supported component ids and all 30 `FaceConfig` paths, including transform/color bounds, collisions, hair/background and body/jersey pairs, all glasses, and every hat substitution outcome. Each case records exact upstream SVG source hashes, display order, transforms, rendered-group hashes, selected 3D package hashes, layer order, and material roles; structured unsupported evidence is generated directly from compatibility. Two in-memory builds are byte-identical, and a clean regeneration matched all 326 public packages containing 594,841 prepared triangles. Validated with `pnpm corpus:facesjs -- --check && pnpm test:facesjs && pnpm prepare:model -- --check` (61 tests).
- B16 | 2026-08-03T11:56:36-03:00 | Added a real-Chromium painted oracle covering 97 paired FacesJS/PolyCSS front cases and eight four-angle volume rows across all 326 supported components, with prepared front/radial visibility transitions and active rotation lighting. Painted front, oblique, side, and rear evidence retains feature clearance and closed silhouettes; the corrected source-facing hemisphere sign keeps head, body, hair, and headwear shells visible on the proper side. The browser report has zero failures, console errors, request failures, canvases, or WebGL elements, while the 22,507-leaf retained case preserves exact identity through 67,090 style writes and 64 publications. Validated with `pnpm oracle:facesjs -- --check`.
- B17 | 2026-08-03T11:59:45-03:00 | Added static, network, DOM, and real-pointer rotation budgets bound to the frozen B1 baseline and the reviewed 326-component quality gain. The generated catalog contains 326 reusable ids and 157 corpus selections with no combination packages; its 137,870,258 bytes stay below a 145 MB ceiling, and the largest selected runtime is 7,399,230 bytes. Real Chromium fetched only the base plus 16 selected components at 4,738,266 cold bytes, then fetched selection deltas for Randomize and a direct config switch. A 90-step drag retained 5,962 leaf identities with zero canvases, active lighting on all leaves, 49,799 style writes, a 1,417-write maximum batch, and 124.9 ms p95. Injected unselected-fetch and ceiling-plus-one controls are rejected. Validated with `pnpm budget:facesjs -- --check && pnpm check`.
- B18 | 2026-08-03T14:02:32-03:00 | Replaced the stale four-style documentation with the tracked 326-supported, zero-unsupported, eight-source-empty matrix; documented all 30 `FaceConfig` paths, source revision/license, reusable selected-only component architecture, adapter-authored depth boundary, prepared retained-DOM lighting/visibility, deterministic generator semantics, build flow, and current limitations. The preview now shows the complete current config with direct `@layoutit/polycss` and `@layoutit/polycss-morph` mounting/deformation imports instead of the cssGraphics wrapper, while the CodePen path remains exact static HTML/CSS. Added a generated JavaScript package entry for the two CSSFace composition helpers used by that example and a test that imports them through the literal `cssface` specifier. Real Chromium visually rendered both panels and the code sample with zero errors and zero canvases. The exact local release command passed 326 catalog packages, 157 corpus cases, 97 paired fronts, eight four-view volume rows, budget checks, all 62 tests, typecheck, and production build. No push, deploy, or publication was performed. Validated with `pnpm check:facesjs-catalog && pnpm corpus:facesjs -- --check && pnpm oracle:facesjs -- --check && pnpm budget:facesjs -- --check && pnpm test:facesjs && pnpm check`.
